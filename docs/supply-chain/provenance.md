# Provenance

Provenance records information about how an artifact was built. It can include source identity, build system, builder identity, inputs, and resulting artifact digest.

Provenance is valuable only when consumers verify it against policy.
