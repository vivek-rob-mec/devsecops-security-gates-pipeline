# API Security

API security testing should validate more than generic vulnerability signatures. Include authorization boundaries, object-level access, authentication/token handling, rate/abuse controls, schema validation, and sensitive-data exposure.
