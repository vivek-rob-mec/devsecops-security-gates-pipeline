# Deployment Security

The deployment stage should consume an already-approved immutable artifact. Avoid rebuilding separately for dev, staging, and production because different outputs weaken assurance about what was tested.
