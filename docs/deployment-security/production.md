# Production

Production release should verify:

- approved commit/release;
- exact artifact digest;
- signature/provenance policy;
- required scan/test gates;
- valid exceptions;
- change/release approval if required;
- rollback capability.
