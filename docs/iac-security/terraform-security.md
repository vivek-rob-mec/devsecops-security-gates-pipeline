# Terraform Security

Check Terraform before `apply` and combine static checks with plan review. Sensitive values should not be committed, and Terraform state must be protected because it can contain secrets even when code does not.
