# Dependency Management

- commit deterministic lockfiles where the ecosystem supports them;
- minimize unnecessary dependencies;
- review new direct dependencies;
- automate safe update PRs;
- monitor end-of-life libraries/runtimes;
- avoid untrusted package sources;
- use internal mirrors/proxies where required;
- preserve an SBOM for released artifacts.
