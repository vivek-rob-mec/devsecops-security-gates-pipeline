# Software Composition Analysis (SCA)

SCA identifies third-party/open-source components and associated risks such as known vulnerabilities and, depending on the tool, licensing/policy issues.

Scan lockfiles and the resolved dependency graph where possible. Container/image scanning complements SCA because the final image may contain OS packages and runtime dependencies not visible in application manifests.
