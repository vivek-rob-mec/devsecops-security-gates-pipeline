# Jenkins

Use Jenkins Credentials (or an external secret manager integration), protected agents, least-privilege service identities, and restricted production deployment permissions. Do not expose secrets through environment dumps or shell tracing.
