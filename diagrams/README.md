# Diagrams

Mermaid source files are stored under `source/` so GitHub can render them and contributors can version them as text. Export PNG/SVG versions for presentations or documentation if desired.
